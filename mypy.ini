[mypy]
python_version = 3.11
ignore_missing_imports = True
# Map package base explicitly to avoid duplicate-module errors
# Format: explicit_package_bases = <package>=<path>
explicit_package_bases = app=.
