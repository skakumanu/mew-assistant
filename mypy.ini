[mypy]
python_version = 3.11
ignore_missing_imports = True
# Per-module ignore for duplicate-module error when mypy sees
# "utils.logging" and "app.utils.logging" for the same file.
[mypy-app.utils.logging]
ignore_errors = True
