# Core FastAPI
fastapi==0.141.1
uvicorn[standard]==0.38.0
pydantic==2.12.4
pydantic-settings==2.15.0

# Database
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.12.0

# Authentication & OAuth
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
argon2-cffi==25.1.0
python-multipart==0.0.32
httpx==0.28.1
cryptography==50.0.1
workos>=10.3.0,<11

# Google Services
google-auth==2.57.1
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.2.0
google-api-python-client==2.199.0

# Azure
azure-identity==1.25.3
azure-keyvault-secrets==4.11.2

# Email
python-dotenv==1.2.3
email-validator==2.3.0

# Templating (Jinja2Templates in app/routers/mew_ui.py and onboarding.py)
jinja2==3.1.6

# Twilio (SMS/WhatsApp webhook signature verification)
twilio==9.11.0

# Other
requests==2.33.0
python-dateutil==2.9.0.post0
pytz==2026.3.post1
bleach==6.4.0
pytest-asyncio==1.4.0
pytest-benchmark==5.3.0

# Transitive deps pinned directly to close CVEs flagged by OSV Scanner.
# None of these are imported by this app's own code - they ride in via
# google-auth-oauthlib, python-jose, workos, httpx, and pytest/rich - but
# leaving them unpinned resolves to whatever old version those pull in, so
# pin them here to force the fixed versions.
aiohttp==3.14.3
ecdsa==0.19.2
idna==3.15
protobuf==5.29.6
pygments==2.20.0
pyjwt==2.13.0
